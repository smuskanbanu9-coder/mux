# mux
A **Multiplexer (MUX)** is a digital circuit that selects one input from multiple inputs and sends it to a single output. The selection is controlled by **select lines**, which determine which input is connected to the output.
